
from setuptools import setup, find_packages
setup(
    name = 'andre_spatial_07',
    version = '0.0.1',
    packages = ['andre_spatial_07'],
    package_dir = {'andre_spatial_07': '.'},
    description = '',
    install_requires = [],
)

